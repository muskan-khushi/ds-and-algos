#include <bits/stdc++.h>
using namespace std;

//connected components
//keep a track of visited array with all vertices while traversing.
//if any vertex remains unvisited, call the traversal algo from that node
//this because we can only traverse components of a connected graph in one go

//for a graph with 10 vertices
int main1(){
    // for (int i=1; i<=10; i++){
    //     if (!visited[i]){
    //         traversal(i); //with mark all nodes as visited in one connected component
    //     }
    // }

    return 0;
}